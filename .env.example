# Supabase — copy to .env and fill in (these are safe public keys).
# In Netlify, set the same two variables under Site settings → Environment variables.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_xxx
