import AdminApp from "@/components/AdminApp";

export const dynamic = "force-dynamic";
export const metadata = { title: "HCR League — Admin" };

export default function AdminPage() {
  return <AdminApp />;
}
